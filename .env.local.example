# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co   # same as SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App (used by server components for internal API calls)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ReliefWeb (required by API policy)
RELIEFWEB_APPNAME=warspy/1.0

# Admin endpoint protection (any long random string)
WAR_SCANNER_ADMIN_TOKEN=change-me-to-a-secret

# Vercel Cron secret — set in Vercel dashboard under Settings → Environment Variables
# Vercel auto-sends this as Authorization: Bearer <CRON_SECRET> on cron requests.
# The GET handler also accepts WAR_SCANNER_ADMIN_TOKEN as fallback.
CRON_SECRET=

# Pipeline config (optional — defaults shown)
INGEST_WINDOW_HOURS=24
INGEST_POLL_MINUTES=3

# Translation (optional — uses free Lingva Translate by default)
GOOGLE_TRANSLATE_API_KEY=   # Google Cloud Translation v2 — higher quality, optional

# Social feed (both optional — core RSS/Reddit sources work without any keys)
NEWSAPI_KEY=            # newsapi.org free tier (100 req/day) — format: your-key-here
TWITTER_BEARER_TOKEN=   # X API free tier — used as bonus source only
